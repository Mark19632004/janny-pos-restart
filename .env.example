# Railway rellenará DATABASE_URL desde su Postgres
DATABASE_URL=

# App
PORT=4000
CANCEL_PIN=1111
# Sirve el build del frontend que copiamos a ./dist
FRONTEND_DIR=./dist

# CORS: en Railway, sirve mismo dominio; deja '*'
CORS_ORIGIN=*
